#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
        int alph;

        for (alph = 0; alph <= 9; alph++)
                putchar(alph, );
        putchar(\n);
        return (0);
}
