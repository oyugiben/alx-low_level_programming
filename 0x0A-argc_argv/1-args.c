#include <stdio.h>
#include <stdlib.h>

/**
 * main - Entry point
 * @argc: number of arguments fed to the command line
 * @argv[]: Pointers to the strings in the command line
 *
 * Return: Always 0
 */
int main(int argc, char* argv[])
{
	printf("%d\n", argc);
	exit(EXIT_SUCCESS);
}
