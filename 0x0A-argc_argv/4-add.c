#include <stdio.h>
#include <stdlib.h>

/**
 * main - entry point
 * @argc: numbers
 * @*argv[]: pointers
 *
 * Return: 0 sometimes
 */
int main(int argc, char *argv[])
{
	if (argc = 1)
	{
		printf("0\n");
		exit(EXIT_SUCCESS);
	}
	else
	{
		if (
